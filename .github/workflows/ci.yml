name: Budget Manager CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: 📦 Install backend dependencies
        working-directory: backend
        run: npm install

      - name: ✅ Run basic check (no tests yet)
        run: echo "✅ CI setup working. Add tests later."
